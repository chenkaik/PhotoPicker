<resources>

    <!-- Base application theme. DarkActionBar -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--底部DialogStyle(背景透明) start-->
    <style name="PhotoPickerDirectoryDialogStyle" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:windowBackground">@color/transparency</item>
    </style>
    <!--底部DialogStyle(背景透明) end-->

</resources>
